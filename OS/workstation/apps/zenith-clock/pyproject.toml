[project]
name = "zenith-clock"
version = "0.1.0"
description = "ZenithOS clock"
requires-python = ">=3.11"
dependencies = [
  "PyGObject"
]

[project.scripts]
zenith-clock = "zenith_clock.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
