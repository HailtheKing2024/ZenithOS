[project]
name = "zenith-files"
version = "0.1.0"
description = "ZenithOS file manager"
requires-python = ">=3.11"
dependencies = [
  "PyGObject"
]

[project.scripts]
zenith-files = "zenith_files.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
