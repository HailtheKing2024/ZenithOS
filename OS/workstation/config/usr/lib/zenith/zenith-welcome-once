#!/bin/sh
set -u

marker="${XDG_CONFIG_HOME:-$HOME/.config}/zenith/welcome-shown"
if [ -f "$marker" ]; then
    exit 0
fi

mkdir -p "$(dirname "$marker")"
touch "$marker"

if command -v zenith-welcome >/dev/null 2>&1; then
    zenith-welcome &
fi

exit 0
